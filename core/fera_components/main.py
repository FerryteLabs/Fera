import os
import curl_cffi

CurrentVersion = 0.01
CurrentServerVersion = 0.01

def IsUpdateAvailable(argc, argv) :
    curl_cffi.Curl
    

def refresh():
    print("everything")
    #IsUpdateAvailable(CurrentVersion, CurrentServerVersion)